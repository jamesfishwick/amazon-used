{
  "name": "cheapest-read",
  "version": "1.0.0",
  "description": "Cheapest Read — Chrome extension that scans your Amazon wishlist and surfaces the lowest total price (item + shipping) across new and used offers for every book on the list.",
  "main": "manifest.json",
  "scripts": {
    "test": "node tests/happy-path.spec.js && node tests/render-label.spec.js && node tests/retry-backoff.spec.js",
    "build:zip": "bash scripts/build-zip.sh",
    "prepare": "husky",
    "lint": "biome check",
    "lint:fix": "biome check --write",
    "format": "biome format",
    "format:fix": "biome format --write"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "playwright": "^1.53.1"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.12",
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0"
  },
  "lint-staged": {
    "*.{js,json}": "biome check --write --no-errors-on-unmatched --files-ignore-unknown=true",
    "*.sh": "shellcheck"
  }
}
